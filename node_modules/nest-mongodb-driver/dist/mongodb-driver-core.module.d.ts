import { DynamicModule, Provider, OnApplicationShutdown } from '@nestjs/common';
import { MongoDbDriverModuleAsyncOptions, MongoDbDriverModuleOptions } from './interfaces';
import { ModuleRef } from '@nestjs/core';
export declare class MongoDbDriverCoreModule implements OnApplicationShutdown {
    private readonly options;
    private readonly moduleRef;
    constructor(options: MongoDbDriverModuleOptions, moduleRef: ModuleRef);
    static forRoot(options: MongoDbDriverModuleOptions, connection?: string): DynamicModule;
    static forRootAsync(options: MongoDbDriverModuleAsyncOptions, connection: string): DynamicModule;
    onApplicationShutdown(): Promise<any>;
    static createAsyncProviders(options: MongoDbDriverModuleAsyncOptions): Provider[];
    static createAsyncOptionsProvider(options: MongoDbDriverModuleAsyncOptions): Provider;
    private static createConnectionFactory;
}
